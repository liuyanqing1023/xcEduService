server:
  port: ${PORT:30010}
spring:
  application:
    name: test-rabbit
#rabbitmq配置
#如果连接失败有可能是用户没有授权,也有可能是没有远程访问的权利
  rabbitmq:
    host: 39.106.190.10
    port: 5672
    username: admin
    password:  admin
    publisher-confirms: true
    virtual-host: /
